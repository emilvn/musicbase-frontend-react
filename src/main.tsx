import React from 'react'
import ReactDOM from 'react-dom/client'
import MusicBase from './components/MusicBase/MusicBase.tsx'
import './styles/index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MusicBase />
  </React.StrictMode>,
)
